{% extends 'base.html.twig' %} {% block title %}Create New Club{% endblock %} {%
block body %} {% include 'components/nav.html.twig' %}
<div class="container">
    <h3 class="mt-5">Create New National</h3>
    <div class="col-lg-6">
        <form action="{{ path('add_club') }}" method="post" class="mt-3">
            <div class="form-group">
                <label for="name">Club Name</label>
                {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</div>
{% endblock %}